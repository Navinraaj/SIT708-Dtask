package com.example.trucksharing;

import android.view.View;

public interface ItemClickListener {
    // Interface to define the click listener for RecyclerView items.
    void onClick(View view, int position);
}
